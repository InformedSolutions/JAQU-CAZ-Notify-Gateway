application:
  title: Notify Gateway
  consume: true
  queue:
    new: notify-gateway_new_dev.fifo
    dlq: notify-gateway_dlq_dev.fifo
    request-limit: notify-gateway_request_limit_dev.fifo
    service-down: notify-gateway_service_down_dev.fifo
    service-error: notify-gateway_service_error_dev.fifo

springfox:
  documentation:
    swagger:
      v2:
        path: /v1/swagger-docs
        
# Note these are default values that will be overriden by aws secrets manager values 
notify-api-key-dev:
notify-api-key-team: